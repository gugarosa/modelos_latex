% Defines the size of the poster
\usepackage[orientation=portrait,size=a0,scale=1.4,debug]{beamerposter}

% Defines the encoding
\usepackage[utf8]{inputenc}

% Languages utilities
\usepackage[brazil]{babel}

% Makes beautiful tables
\usepackage{booktabs}

% EPS to PDF
\usepackage{epstopdf}