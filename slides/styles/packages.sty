% Defines the encoding
\usepackage[utf8]{inputenc}

% Languages utilities
\usepackage[brazil]{babel}

% Resets the slide counter when entering the appendix
\usepackage{appendixnumberbeamer}

% Makes beautiful tables
\usepackage{booktabs}

% EPS to PDF
\usepackage{epstopdf}